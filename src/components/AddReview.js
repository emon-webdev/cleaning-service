import React from 'react';
import useTitle from '../hooks/useTitle';

const AddReview = () => {
    useTitle('Add Review')
    return (
        <div>
            <h2>Add Review</h2>
        </div>
    );
};

export default AddReview;